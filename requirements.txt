# --- Base Utilities ---
python-dotenv==1.0.0      # Loads .env files (for local dev)
requests==2.31.0          # For making HTTP calls to your other services
urllib3==2.0.7            # Required by requests
# --- Database Drivers ---
psycopg2-binary==2.9.9    # For PostgreSQL
# sqlalchemy==2.0.25      # If you use an ORM
# redis==5.0.1            # If you use Redis for caching
